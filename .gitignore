/target/
/Packages/NMPNativeRuntime/.build/
/Packages/NMPNativeRuntime/NMPNativeRuntime.xcframework/
*.swp
.DS_Store
__pycache__/
*.pyc

# Xcode derived data from local build/test gates.
/.build-derived/
/.build-derived-ios/

# Redb stores the test suites open in the crate directory rather than a
# temporary one. Untracked and unignored, these are one `git add -A` away
# from being committed.
*.redb
*.redb.lock
